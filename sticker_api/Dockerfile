# Base image
FROM <base-image>

# Set working directory
WORKDIR <working-directory>

# Copy files into container
COPY <source> <destination>

# Run commands to set up container
RUN <command>

# Expose ports
EXPOSE <port>

# Set environment variables
ENV <variable> <value>

# Start command
CMD <command>
